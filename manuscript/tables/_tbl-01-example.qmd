<!-- Include partial, not a page (see CLAUDE.md's "Standalone submission
     exports"). Needs its own library(flextable) - without it the object
     prints as a dumped internal structure. -->

```{r}
#| label: tbl-01-example
#| tbl-cap: "Example table generated from the pipeline."
library(flextable)
source(here::here("R/functions.R")) # not auto-sourced into Quarto's session
targets::tar_read(tbl_01_example, store = here::here("_targets")) |>
  flextable_use_format_font()
```
