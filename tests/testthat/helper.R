# tests/testthat/helper.R ----
# Auto-loaded by testthat::test_dir()/test_file() before any test runs.
# Sources R/functions.R once for every test file - the substitute for
# library(pkg) since there's no package here to load (see CLAUDE.md's
# "an include partial needs its own library() call" gotcha for the same
# underlying reason this can't be assumed already loaded).
library(here)
here::i_am("tests/testthat/helper.R")
source(here("R/functions.R"))
