.Rproj.user
.Rhistory
.RData
.Ruserdata

**/.quarto/
**/*.quarto_ipynb

# Not root-anchored: output-dir must resolve inside manuscript/'s own
# project directory (manuscript/output/), not the repo root - see
# CLAUDE.md's "Directory layout" for why output-dir: ../output was a
# genuinely broken configuration, not just relocated for tidiness.
**/output/
/submission/
/_targets/
/data/
# Not root-anchored: manuscript/ is its own nested Quarto project with its
# own _freeze/ cache, one level down from the repo root - see CLAUDE.md's
# "Directory layout".
**/_freeze/*

!meta
meta/*
!meta/meta
/.luarc.json
# Typst will occasionally (Flextable behaviour?) generate temporary
# directories with intermediate images in them.
**/figure-typst/*

